<?xml version="1.0" encoding="utf-8"?>
<project>
	
	<ndll name="diskspace" if="ios" />
	
	<!-- Use the following for an Android Java extension, not needed otherwise -->
	
	<dependency name="enoughspace" path="dependencies/diskspace" if="android" />
	<android extension="com.diskspace.DiskSpace" />
	
</project>
